# Screenshots

Drop product screenshots / GIFs here and reference them from the root `README.md`.

Suggested captures (with the stack running via `docker compose up -d` + `npm run dev`):

| File | What to capture |
| --- | --- |
| `login.png` | Login screen |
| `employee-dashboard.png` | Employee dashboard with the **Vacation Balance** card |
| `admin-metrics.png` | Admin → **Metrics** tab (cards + bars) |
| `admin-calendar.png` | Admin → **Team Calendar** tab |
| `mailhog.png` | MailHog inbox showing an approval email |

Tip: a short GIF of "employee requests → admin approves → email appears in MailHog" makes a great hero image.
